This is a simple project related to bank management system. In this system Mysql database is used and the
 name of database is "bank".

All the required files are attached.

----------manual to run this system---------------

First xampp is downloaded and database named "bank" is created and you have to import
the sql file that is named as customer then opened the folder of project in any IDE ( I used IntelliJ).
 you need to import the sql connector in library to use this system.

The jar file of connector is also attached on this file.
